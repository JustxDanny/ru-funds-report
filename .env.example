# Copy to .env and fill in. Never commit the real .env.

# Created via @BotFather in Telegram.
TELEGRAM_BOT_TOKEN=

# Comma-separated list of chat IDs that should receive the report.
# Each recipient must /start the bot first; then `python scripts/get_chat_ids.py`
# prints their numeric chat_id.
TELEGRAM_CHAT_IDS=
